import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  tools: "Tools",
  agents: "Agents",
  workflows: "Workflows",
  assistant: "Agentron (Chat)",
  heap: "Heap (specialists)",
};

export default meta;
