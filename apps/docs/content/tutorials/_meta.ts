import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  "first-agent": "First agent (prompts)",
  "first-workflow": "First workflow (prompts)",
  "first-rag": "First RAG (prompts)",
};

export default meta;
