/**
 * Canonical URL for the documentation site (Nextra, deployed at docs.agentron.rocks).
 * Use this for any in-app links to the docs so they stay correct if the host changes.
 */
export const DOCS_URL = "https://docs.agentron.rocks";
